server:
  port: 8080

realty:
  rapidapi:
    host: realty-in-us.p.rapidapi.com
    key: ${RAPIDAPI_KEY:}   # Recommended to inject via environment variable
  aggregation:
    detailTimeoutSec: 20
    perPageEnrichLimit: 20   # Maximum concurrent enrichment per page to avoid upstream rate limits
